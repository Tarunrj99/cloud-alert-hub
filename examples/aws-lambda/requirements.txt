# ---------------------------------------------------------------------------
# Install the cloud_alert_hub library directly from your public GitHub repo.
# Replace `Tarunrj99` with your GitHub handle.
#
# For production, pin to a tag (e.g. @v0.1.0) or a commit SHA so deploys are
# reproducible. `@main` is convenient for quick iteration only.
# ---------------------------------------------------------------------------
git+https://github.com/Tarunrj99/cloud-alert-hub.git@v0.1.0#egg=cloud-alert-hub
